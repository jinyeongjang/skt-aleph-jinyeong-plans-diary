# Project Definition & AI Agent Guidelines: SKT ALEPH 과제 6 — 플랜두씨 다이어리 1

이 문서는 본 프로젝트의 핵심 품질 기준, 인수인계 규격, 레이아웃 및 과제 통과 기준(T06-C01 ~ T06-C83)을 영구적으로 정의하고 강제하는 프로젝트 정의 파일(Rules Specification)입니다.
향후 모든 코드 수정, 신규 기능 추가 및 스타일 변경 시 본 문서에 명시된 규칙을 반드시 준수해야 합니다.

---

## 1. 기본 아키텍처 & 디자인 원칙

- **무로그인 공개 정적/동적 웹 (T06-C01)**: 계정 생성, 로그인, 인증, 초대, 비밀번호, OAuth, CAPTCHA 없이 브라우저 새 시크릿 창에서 누구나 즉시 다이어리, 통계 집계, 검사 결과를 확인하고 테스트할 수 있어야 합니다.
- **첫 화면 공개 안내 필수 고지 (T06-C82)**:
  - 첫 화면 상단에 _"지금은 로그인이 없어 링크를 아는 사람은 누구나 볼 수 있습니다. 남이 봐도 괜찮은 내용만 넣으세요"_ 안내 문구가 상시 표기되어야 합니다.
- **실제 데이터베이스 영속성 (T06-C34, T06-C35)**:
  - 단순 클라이언트 메모리/임시 저장에 그치지 않고, PostgreSQL (Supabase) 및 영속 스토어에 연결되어 브라우저를 새로고침해도 ID·날짜·값·단위가 100% 동일하게 복원되어야 합니다.
- **계약 스키마 준수**: `contracts/pds-schema-v2.json`의 표, 항목, 관계, 날짜 규칙을 엄격히 준수합니다.
- **모노크롬 & 글래스모피즘 스타일**: 다크/라이트 모드를 모두 지원하는 정돈된 글래스모피즘(`backdrop-blur`, 은은한 보더, 앰비언트 광원 효과)을 유지합니다.
- **품질 자동화**: 코드를 변경한 후에는 반드시 다음 검증을 통과해야 합니다.
  - `npm test`: 사전 고정 10대 검사 전수 100% 통과 (10/10 PASS)
  - `npm run format`: Prettier 포맷팅 준수
  - `npm run lint`: Oxlint 오류 및 경고 0건
  - `npm run build`: TypeScript 컴파일 및 Vite 번들링 0 오류

---

## 2. 카드별 핵심 구현 규격

### 카드 1 — 계획 세우기 (T06-C04 ~ T06-C08)

- 계획 하나에 기간(`T06-C04`), 우선순위(`T06-C05`), 성공 기준(`T06-C06`), 예상 시간(`T06-C07`)을 담아 저장합니다.
- **계획 수정 시 원본 보존 (`T06-C08`)**: 계획을 수정해도 처음 세운 원본 계획 스냅샷이 `plan_revisions` 테이블에 아카이빙되어 열람 가능해야 합니다.

### 카드 2 — 할 일 다루기 (T06-C09 ~ T06-C20)

- 계획에 딸린 할 일을 생성(`T06-C09`), 수정(`T06-C10`), 완료(`T06-C11`), 진행중 되돌리기(`T06-C12`), 삭제(`T06-C13`)할 수 있어야 합니다.
- 마감일(`T06-C14`), 우선순위(`T06-C15`), 태그(`T06-C16`), 예상 시간(`T06-C17`)을 저장합니다.
- 키워드 검색(`T06-C18`), 조건 필터링(`T06-C19`)을 지원합니다.
- **정렬 기준 화면 명시 (`T06-C20`)**: 화면에 "정렬 기준: 1차 마감일 빠른 순 ➔ 2차 우선순위(높음>보통>낮음) ➔ 3차 등록순"을 명시하고 그 기준대로 정렬합니다.

### 카드 3 — 실제로 한 일 적기 (T06-C21 ~ T06-C27)

- 실행 기록에 시작 시각(`T06-C23`), 끝난 시각(`T06-C24`), 실제로 걸린 시간(`T06-C25`), 막혔던 이유(`T06-C26`)를 저장합니다.
- **계획값 불변 (`T06-C27`)**: 실행 기록을 저장해도 할 일/계획의 원래 예상 시간 등 계획 값은 변조되지 않습니다.
- **완료 버튼 연타 멱등성 (`T06-C21`, `T06-C22`)**: 완료 버튼을 연달아 두 번 눌러도 `idempotency_key` 제약으로 인해 실행 기록은 1건만 남고 완료 집계도 정확히 1만 늘어납니다.

### 카드 4 — 돌아보기, 그리고 다음 계획으로 (T06-C28 ~ T06-C33, T06-C83)

- 계획 할 일 수(`T06-C28`), 완료 수(`T06-C29`), 마감 지연 수(`T06-C30`, 완료 건 중복 배제), 막힘 수(`T06-C31`), 시간 오차(실제-예상) 분석(`T06-C32`)을 제공합니다.
- **집계 숫자 클릭 드릴다운 (`T06-C83`)**: 대시보드 통계 숫자를 클릭하면 해당 근거 기록 할 일 목록으로 이동합니다.
- **다음 계획으로 피드백 연계 (`T06-C33`)**: 돌아보기에서 정한 고칠 점 한 건이 신규 계획 생성 폼의 성공 기준에 자동 승계됩니다.

### 카드 5 — 내 것으로 채우고, 잃지 않게 (T06-C34 ~ T06-C36, T06-C57 ~ T06-C82)

- 실제 개인 계획 1개 이상(`T06-C78`), 할 일 5개 이상(`T06-C79`), 실행 기록 3개 이상(`T06-C80`)이 들어있고 집계 숫자가 0이 아니어야 합니다(`T06-C81`).
- 전체 자료를 단일 JSON 파일로 내보낼 수 있어야 합니다(`T06-C36`).
- XSS 악성 스크립트 문자열이 태그로 실행되지 않고 텍스트 그대로 보여야 합니다(`T06-C57`).
- 비밀키 원문 노출이 없어야 합니다(`T06-C58`).
- 짧은 확인 방법 4줄(`T06-C59`)과 AI 판단 3줄(`T06-C60`)이 문서 및 모달에 구비되어야 합니다.
